name: tests
on:
  push:
    branches: ['*']
  pull_request:
    branches: ['*']
jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: setup python
      run: |
        #pip3 install -r requirements.txt
        pip install sqlalchemy
        pip install psycopg2
    - name: docker
      run: |
        git submodule init
        git submodule update
        docker-compose up -d --build
        docker ps -a
        sleep 20

        #        sh load_tweets.sh
        #        docker-compose exec -T postgres ./run_tests.sh
